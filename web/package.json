{
  "name": "freecadclaude-device-ui",
  "version": "0.1.0",
  "private": true,
  "description": "The device-annotation web app. Built into freecad/freecadclaude/device_ui/, which is committed -- users install the addon as a plain file copy with no Node toolchain.",
  "license": "LGPL-2.1-or-later",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "test": "vitest run"
  },
  "dependencies": {
    "perfect-freehand": "^1.2.2"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^7.1.12",
    "vitest": "^3.2.4"
  }
}
