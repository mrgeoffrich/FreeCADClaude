# SPDX-License-Identifier: LGPL-2.1-or-later
#
# ClaudeChat workbench - App (non-GUI) init.
#
# This file is executed at startup for BOTH the GUI and the console
# (FreeCADCmd) builds, so it must not import any GUI modules. There is
# nothing to do on the App side for the milestone-1 skeleton; the panel
# is registered from InitGui.py.
